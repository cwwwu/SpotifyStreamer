<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <item name="actionBarStyle">@style/SpotifyStreamerActionBar</item>
    </style>
    
    <style name="SpotifyStreamerActionBar"
        parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/actionBarSpotifyGreen</item>

        <!-- Support library compatibility -->
        <item name="background">@color/actionBarSpotifyGreen</item>
    </style>

</resources>
